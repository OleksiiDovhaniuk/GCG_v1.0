 #:import atlas design.default_theme
 #:import utils kivy.utils


<MenuBar>:
    size_hint: 1, None
    height: 24
    canvas.before:
        Color:
            # TODO: Set color as part of default_theme (menu_bar_bg)
            rgb: utils.get_color_from_hex('#FFFFFF')
        Rectangle:
            pos: self.pos
            size: self.size

<ToolBar>:
    orientation: 'vertical'
    size_hint: 1, None
    height: 26
    BoxLayout:
        size_hint: 1, None
        height: 1
        canvas.before:
            Color:
                # TODO: Set color as part of default_theme (menu_bar_bg)
                rgb: utils.get_color_from_hex('#4F4F4F')
            Rectangle:
                pos: self.pos
                size: self.size
    BoxLayout:
        id: container
        size_hint: 1, None
        height: 24
        canvas.before:
            Color:
                # TODO: Set color as part of default_theme (menu_bar_bg)
                rgb: utils.get_color_from_hex('#56CCF2')
            Rectangle:
                pos: self.pos
                size: self.size

        

    BoxLayout:
        size_hint: 1, None
        height: 1
        canvas.before:
            Color:
                # TODO: Set color as part of default_theme (menu_bar_bg)
                rgb: utils.get_color_from_hex('#4F4F4F')
            Rectangle:
                pos: self.pos
                size: self.size

<InfoBar>:
    orientation: 'vertical'
    size_hint: 1, None
    height: 24
    BoxLayout:
        size_hint: 1, None
        height: 1
        canvas.before:
            Color:
                # TODO: Set color as part of default_theme (menu_bar_bg)
                rgb: utils.get_color_from_hex('#E0E0E0')
            Rectangle:
                pos: self.pos
                size: self.size
    BoxLayout:
        id: container
        size_hint: 1, None
        height: 23
        canvas.before:
            Color:
                # TODO: Set color as part of default_theme (menu_bar_bg)
                rgb: utils.get_color_from_hex('#FFFFFF')
            Rectangle:
                pos: self.pos
                size: self.size